"use client"

import DePueProjectSetup from "@/components/depue-project-setup"

export default function DePueSetupPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-900 via-emerald-900 to-teal-800 p-6">
      <div className="max-w-7xl mx-auto">
        <DePueProjectSetup />
      </div>
    </div>
  )
}
